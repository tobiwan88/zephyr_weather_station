manifest:
  defaults:
    remote: upstream

  remotes:
    - name: upstream
      url-base: https://github.com/zephyrproject-rtos

  projects:
    - name: zephyr
      remote: upstream
      revision: v4.3.0
      import: true  # Import all modules for now to resolve missing symbols

  self:
    path: zephyr_weather_station
